{
  "//": "Hosting left this file on 28/08/2026 — the site is on Cloudflare Workers (see wrangler.jsonc). What remains is the Firestore block, and it has to remain: the firebase CLI refuses to run at all without a firebase.json ('Not in a Firebase app directory'), so deleting this file would make the documented retirement step — firebase deploy --only firestore:rules — impossible to run, leaving the retired collection open to writes from stale cached bundles.",
  "firestore": {
    "rules": "firestore.rules"
  }
}
