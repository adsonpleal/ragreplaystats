node_modules/
dist/
.DS_Store
*.log
*.local
.env*

# Local Claude state — but skills are project documentation and do ship.
.claude/*
!.claude/skills/

# Firebase Admin service account keys — keep these out of the repo.
*-firebase-adminsdk-*.json
service-account*.json

# Sample replay — drop your own .rrf in the browser instead
rag_test.rrf

